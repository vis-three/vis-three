import { VisCompiler } from "./VisCompiler";

export class VisHelperCompiler implements VisCompiler {

  constructor () {

  }
  
  initCompile(): this {
    return this
  }

  disposeCompile(): this {
    return this
  }
}