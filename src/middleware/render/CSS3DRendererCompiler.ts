import { Compiler } from "../../core/Compiler";

export class CSS3DRendererCompiler extends Compiler {
  constructor () {
    super()
  }

  setTarget(): this {

    return this
  }

  compileAll(): this {
  

    return this
  }

  dispose (): this {
    return this
  }
}