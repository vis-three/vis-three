import { Compiler } from "@vis-three/middleware";
import { ConstraintorConfig } from "./ConstraintorConfig";

export class ConstraintorCompiler extends Compiler<ConstraintorConfig, object> {
  constructor() {
    super();
  }
}
