import { Path, TubeGeometry } from "three";
export declare class PathTubeGeometry extends TubeGeometry {
    constructor(path?: Path, tubularSegments?: number, radius?: number, radialSegments?: number, closed?: boolean);
}
